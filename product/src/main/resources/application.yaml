spring:
  datasource:
    username: master
    password: 981221
    url: jdbc:mysql://localhost:3300/gulimall_pms
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:11148
  application:
    name: member
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto