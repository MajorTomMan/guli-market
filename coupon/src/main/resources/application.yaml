spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.253.131:8848
  application:
    name: coupon
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
